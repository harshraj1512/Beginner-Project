import React, { useEffect, useState } from 'react'

function APIS() {
    const [fetchapi,setFchAPI] = useState();

    useEffect (async)  ={
        const res = await axios.get();
        
    }
  return (
    <div>APIS</div>
  )
}

export default APIS